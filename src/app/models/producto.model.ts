export interface Producto {
  id: string;
  orderId: string;
  sku: string;
  name: string;
  quantity: number;
  purchaseDate: string;
  warrantyUntil: string;
  unitPrice: number;
  currency: string;
}
